# Production environment (Proxmox prod VM, deployed on tagged release with approval)
#
# NOTE: the in-memory seed accounts in SecurityConfig are a Week 1 convenience.
# Before any real prod release (Week 11), authentication must be JPA-backed
# and the seed accounts removed — this is checked in the release review.
server:
  port: 8080

spring:
  thymeleaf:
    cache: true
